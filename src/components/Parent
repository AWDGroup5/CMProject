/*
<template>
  <div class="container">
    <h1 class="title">???</h1>
    //chicd components that recives data as prop
    <child component Table :data="prop"/>
  </div>
</template>

<script>
import firebase from 'firebase/app';
import 'firebase/database';
// import the table from the app.vue.
import Table from './app.vue?';

export default {
  components: {
    Table,
  },
  //define a data that return an empty object
  data() {
    return {
      Object: {},
    };
  },
  //defined a mounted function that sets up a listener on the node  of the database using the "on" method.
  mounted() {
    const db = firebase.database();
    const nodeRef = db.ref('Data');

    nodeRef.on('value', snapshot => {
      this.Object = snapshot.val();
    });
  },
};
</script>

<style>
.container {
  max-width: 600px;
  margin: 0 auto;
  padding: 40px;
  text-align: center;
}

.title {
  font-size: 36px;
  font-weight: bold;
  margin-bottom: 20px;
}
</style>
*/